# task-1
>> I have imported BMW sales data (2010-2024) in CSV format with the help of pandas library and explored basic information about data using df.head() pandas built-in function

>> got the info about columns data types using df.dtypes and checked for any null values to replace or drop them but fortunately i found none

>> Changed the data type of Mileage_KM column from int to float

>> Converted the categorical value into numerical value to avoid multicollinearity 

>> with the help of seaborn and matplotlib libraries Visulize and plotted box-plots of Sales_Volume column